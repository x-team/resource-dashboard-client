<ul class="list-group">
  {{#each opportunities as |opportunity|}}
    {{opportunity-list-item model=opportunity}}
  {{/each}}
</ul>

{{pagination-bar
  page=page
  totalItemsCount=totalItemsCount
  onSelect=(action 'selectPage')
}}
